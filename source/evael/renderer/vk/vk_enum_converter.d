module evael.renderer.vk.vk_enum_converter;

import evael.renderer.enums;
import bindbc.opengl;

static class VulkanEnumConverter
{
	@nogc
	public static uint bufferType(BufferType type) nothrow
	{
		return 0;
	}

	@nogc
	public static uint shaderType(ShaderType type) nothrow
	{
		return 0;
	}

	@nogc
	public static uint attributeType(AttributeType type) nothrow
	{
		return 0;
	}

	@nogc
	public static uint blendFactor(BlendFactor blendFactor) nothrow
	{
		return 0;
	}

	@nogc
	public static uint blendFunction(BlendFunction blendFunction) nothrow
	{
		return 0;
	}
}